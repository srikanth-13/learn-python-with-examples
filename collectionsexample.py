#Python Collections(Arrays).
#List[] is collection which is ordered and changeable. Allows duplicate members.
#Tuple() is collection which is ordered and unchangeable. Allows duplicate members.
#Set is collection which is unordered , unchangeable and unindexed. No duplicate members.
#Ditionary{} is a collection which is ordered and changeable. No duplicate members.
"""
1.List[]=>

Allows Duplicate,
Any type of data can be stored.
We can modify the list item.
We can add or remove
insert(),append(),extend(),pop().
Example:

a=[1,2,3,4,5]
print(a)
a.append(8)
print(a)
a.insert(2,11)
print(a[4])
print(a)
a.pop()
print(a)
a.extend([7,9])
print(a)
"""

"""
2.Tuple()==>
Allows Duplicate
Any type of data can be stored.
 We cannot Modify the tuple item.

 We can not add or remove.
 """
"""
3.set{}==>

Do not allow duplicate, duplicate values will be removed.
Any type of data can be stored
We cannot modify the set item but we can add or remove items.
sets or unordered.
filename.txt
"""
"""
4.Dictinary{}==>
Do not allow duplicate, duplicate values will be overwrite existing value.
Any type of data can be stored.
Key:value pair{"name":"Emc"}

get(),key(),values(),items(),update({"year":2009}),thisdict["color"]= "red",thisdict.pop("model"),del,clear().

Exmple:
a={
    "Name":"srikanth",
    "Age":25,
    "dharmapuri":"location",
     "studentd": ["bala","john"]
   }
print(a.keys())
print(a.values())
"""










